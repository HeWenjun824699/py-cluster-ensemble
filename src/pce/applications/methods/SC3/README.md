  总结现在的状态：
   1. 接口层 (`sc3.py`)：严格复刻 R 的 sc3() 参数，移除 nRepeat，移除对 Y 的依赖。
   2. 核心层 (`methods.SC3`)：
       * __init__：执行基因过滤（基于0值比例），采样训练集（若数据量大）。
       * estimate_k：使用 Tracy-Widom 理论自动估计 $k$。
       * run：执行标准的 SC3 流程（距离 -> 变换 -> K-Means -> 共识 -> SVM预测 -> 生物学分析）。

  这套实现现在是完全且严格对标 R 语言官方源码的。